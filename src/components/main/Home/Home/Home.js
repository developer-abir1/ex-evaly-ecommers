import React from 'react';
import Layout from '../../Layout/Layout';
import Products from '../products/Products';

const Home = () => {
    return (
        <Layout>
            <Products/>
        </Layout>
    );
};

export default Home;